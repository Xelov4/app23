const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const tagCount = await prisma.tag.count(); console.log('Nombre de tags: ' + tagCount); const categoryCount = await prisma.category.count(); console.log('Nombre de catégories: ' + categoryCount); const tagsOnToolsCount = await prisma.tagsOnTools.count(); console.log('Nombre d\'associations tags-outils: ' + tagsOnToolsCount); const categoriesOnToolsCount = await prisma.categoriesOnTools.count(); console.log('Nombre d\'associations catégories-outils: ' + categoriesOnToolsCount); } main().then(async () => { await prisma.$disconnect(); }).catch(async (e) => { console.error(e); await prisma.$disconnect(); process.exit(1); });
